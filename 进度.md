# 点问 CSS Whisperer · 进度

> 唯一进度文档。最后更新 2026-07-26，版本 v0.7.0（待用户真机验证）。

## 当前状态

功能较完整。v0.1.0→v0.6.1 均已真机验证通过。**v0.7.0（CodeMirror 等 6 项改动）刚写完，等用户自己真机验证。**

## 本次会话交接摘要

- **本次目标**：修 6 个 bug（用户报的第 1-6 条）。
- **完成了什么**：
  1. AI 改了所有图标 → 提问强调"精确选择器、别用通用类"，locator 生成唯一路径。
  2. 误点撤回 → 编辑器加"重做"(用 CodeMirror 原生 undo/redo)。
  3. 自定义只有按钮没输入框 → 选"自己描述"弹出文字输入框。
  4. 手机端点插件收起抽屉 → 面板/圆球吞 touchstart + pointerdown。
  5. 搜索打字就跳转 → 改成打字只更新计数/高亮，回车/点▼才跳。
  6. 搜索高亮抖动+滚动卡 → **引入 CodeMirror(CDN 5.65.15)** 替换自研 textarea+叠层方案。
- **遗留/待验**：v0.7.0 全部改动等用户真机验证，尤其 **CodeMirror 能否从 CDN 加载成功**（最大风险）。
- **下一步**：用户已拍板 → **引入 PostCSS 重构 analyzer**（真 AST 解析替代正则）。

## 真机验证清单（v0.7.0）

> 改了 manifest 版本号，需**重启 ST 进程**再验（否则浏览器用缓存旧文件）。

1. **[最优先] CodeMirror 加载**：✏️编辑 → 出现带行号的代码编辑器（非纯文本框）？CSS 有语法着色？
   - 失败会显示"编辑器需要联网加载，当前用简易模式" → CDN 没连上，需告知我改打包方案。
2. **滚动流畅**：上下滚动编辑器不卡、不抖（第6条核心痛点）。
3. **搜索不抢焦点**：搜索框打字光标留在框内、不跳编辑区；计数显示"共 N"；回车/▼才跳转高亮。
4. **撤回/重做/重置**：改内容→撤回能退→重做能恢复→重置回打开时。
5. **自定义输入框**：改什么选"自定义"或"自己描述"→ 弹出文字输入框可打字。
6. **精确选择器**：点顶栏图标类元素→生成提问→含"精确选择器（就指这一个）"+"别用 .drawer-icon 通用类"。
7. **手机端防收起**：移动视图→展开顶栏抽屉→点插件→抽屉保持展开。

## 已知技术债（详见 HANDOVER.md）

1. analyzer 靠正则+遍历 styleSheets，非 AST → **待引入 PostCSS**（已决定）。
2. CodeMirror 走 CDN，需联网（有 textarea 兜底）。
3. 精确改动/AI 是否照做依赖 AI 配合。

## 未来路线（优先级）

1. PostCSS 重构 analyzer（已拍板）。
2. 接入 API 直接改 CSS（需第二套提示词 + 复用撤回做安全垫 + API 合规提醒）。
3. CodeMirror 打包进仓库（若 CDN 不稳）。
